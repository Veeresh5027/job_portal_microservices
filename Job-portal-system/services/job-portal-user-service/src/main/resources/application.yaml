server:
  port: 5001

spring:
  application:
    name: job-portal-user-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/ai_job_portal_user
    username: root
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver